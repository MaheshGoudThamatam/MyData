'use strict';

exports.load = function(swagger, parms) {

  var searchParms = parms.searchableOptions;
/*
  var list = {
    'spec': {
      description: 'Mentor operations',
      path: '/mentor',
      method: 'GET',
      summary: 'Get all Mentors',
      notes: '',
      type: 'Mentor',
      nickname: 'getMentors',
      produces: ['application/json'],
      params: searchParms
    }
  };

  var create = {
    'spec': {
      description: 'Device operations',
      path: '/mentor',
      method: 'POST',
      summary: 'Create mentor',
      notes: '',
      type: 'Mentor',
      nickname: 'createMentor',
      produces: ['application/json'],
      parameters: [{
        name: 'body',
        description: 'Mentor to create.  User will be inferred by the authenticated user.',
        required: true,
        type: 'Mentor',
        paramType: 'body',
        allowMultiple: false
      }]
    }
  };

  swagger.addGet(list)
    .addPost(create);
*/
};
